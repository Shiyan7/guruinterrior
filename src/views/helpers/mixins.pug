mixin icon(name,mod)
  if(mod)
    svg(class="svg-icon" + ' ' + mod)
      use(xlink:href="./img/sprites/sprite.svg#" + name)
  else
    svg(class="svg-icon")
      use(xlink:href="./img/sprites/sprite.svg#" + name)